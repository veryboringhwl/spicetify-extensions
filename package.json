{
  "name": "theme",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "release": "bun tasks/release.js",
    "dev-build": "bun tasks/build.js --dev",
    "build": "bun tasks/build.js",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "check": "biome check --fix --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "chokidar": "^4.0.3",
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.2",
    "esbuild-plugin-external-global": "^1.0.1",
    "sass": "^1.87.0",
    "ws": "^8.18.1"
  }
}
