{
  "name": "spicetify-extensions",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "release": "bun tasks/release.js",
    "dev-build": "bun tasks/build.js --dev",
    "build": "bun tasks/build.js",
    "dev-watch": "bun tasks/watch.js --dev",
    "watch": "bun tasks/watch.js",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "check": "biome check --fix --unsafe"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "chokidar": "^4.0.3",
    "esbuild": "^0.25.5",
    "esbuild-plugin-external-global": "^1.0.1",
    "typescript": "^5.8.3",
    "ws": "^8.18.2"
  }
}
